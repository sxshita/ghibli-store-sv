<!doctype html>
<html lang="en">
  <body>
    <nav class="navbar sticky-top navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid d-flex justify-content-between">
            <div class="navbar-brand" href="#">
                <img src="../clipart981146.png" alt="" width="85" height="85">
                Studio Ghibli Store
            </div>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
            </button>
            <div class="nav navbar-right">
                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                        <a class="nav-link color-pink" href="/cart">
                            <a role="button" class="btn btn-outline-primary custom-btn" href="/cart" style="margin-top: 10px;">Ir al carrito</a>
                        </a>
                        <a class="nav-link color-pink" href="/">
                            <a role="button" class="btn btn-outline-primary custom-btn" href="/" style="margin-top: 10px;">Volver al inicio </a>
                        </a>
                    </ul>
                </div>
            </div>
        </div>
    </nav>

    <div class="container text-center col-6 mx-auto">
        <div class="card shadow-lg" style="border-color: transparent; margin-top: 40px; width: 900px; height: 700px; background-color: rgba(255, 255, 255, 0.952)">
            <div class="card-header">
                <h2>Comprar productos</h2>
            </div>
            <div class="card-body overflow-auto">
                <div id="products" class="row g-2">
                    {{#if prods}}
                        {{#each prods}}
                            <div class="col-4 mb-4">
                                <img src={{this.thumbnail}} alt={{this.title}} style="width: 150px; height:150px">
                                <p class="">{{this.title}}</p>
                                <button type="button" class="btn btn-primary btn-sm" onclick="return agregarAlCarrito('{{this._id}}')">🛒 Agregar</button>
                            </div>
                        {{/each}}
                    {{else}}
                    <p>No hay productos disponibles.</p>
                    {{/if}}
                </div>  
            </div>
        </div>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/js/bootstrap.bundle.min.js" integrity="sha384-pprn3073KE6tl6bjs2QrFaJGz5/SUsLqktiwsUTF55Jfv3qYSDhgCecCxMW52nD2" crossorigin="anonymous"></script>
  </body>
</html>